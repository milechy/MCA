name: Phase 4 Telegram Smoke Safety

on:
  pull_request:
    branches:
      - infra/phase0-autonomous-foundation
  push:
    branches:
      - infra/phase0-autonomous-foundation

permissions:
  contents: read

jobs:
  telegram-smoke-safety:
    name: Telegram smoke safety tests
    runs-on: ubuntu-latest
    timeout-minutes: 10

    env:
      CI: true
      RALPH_TELEGRAM_RUN_ALL_ENABLED: ""
      TELEGRAM_CHECK_ENV_ALLOW_MISSING: "true"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run CI-safe Telegram smoke helpers
        run: npm run telegram:ci-smoke

      - name: Run Ralph tests
        run: npm run test:ralph

      - name: Run Telegram tests
        run: npm run test:telegram
