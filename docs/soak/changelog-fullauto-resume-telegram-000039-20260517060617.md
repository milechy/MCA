Added a fullauto auto-approver that automatically green-lights low-risk soak operations without manual review, reducing idle time during long-running regression suites. Introduced resume-after-security-stop logic so that soak tests can continue from the last checkpoint after a security-related halt rather than restarting from scratch. Added Telegram bot commands `/resume-request` and `/resume-status`, allowing operators to trigger and monitor soak job resumptions directly from chat without touching the dashboard.
