PR #46 introduces dispatcher hardening and reliability improvements by rewriting the worktree prompt to deliver bounded, deterministic context per execution, swapping the classify-and-order pipeline so that intent classification precedes ranking (reducing redundant ordering work on low-confidence paths), and downgrading soft timeouts from fatal errors to graceful degradation states that preserve partial results and allow retry without losing intermediate progress.
